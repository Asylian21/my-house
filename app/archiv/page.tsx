import Link from '@/app/project-link';
import { Archive, ArrowUpRight } from 'lucide-react';
import { ProjectNav } from '../project-nav';
import '../globals.css';
import '../project.css';
export const metadata={title:'Dom · Archív návrhov'};
const studies=[['existing','Východiskový pôdorys'],['a','A · Predsieň'],['b','B · Priechodný šatník'],['c','C · Dispozičná štúdia'],['d','D · Súkromná spálňa'],['e','E · Zalomenie a krytý zárez']];
export default function ArchivePage(){return <div className="project-app"><ProjectNav active="archive"/><main className="project-content"><div className="project-heading"><div><p className="project-eyebrow">HISTÓRIA PROJEKTU</p><h1>Archív návrhov.</h1><p>Predchádzajúce varianty zostávajú dostupné. Hlavný návrh je C/B/B.</p></div><Archive size={40} strokeWidth={1}/></div><section className="archive-section"><h2>Dispozičné štúdie</h2><p>Zachované pracovné alternatívy s pôvodnými ovládacími prvkami.</p><div className="archive-grid">{studies.map(([id,title])=><Link key={id} href={`/archiv/podorys?variant=${id}&mode=study&heating=a&living=a`}><span>ARCHÍV / {id.toUpperCase()}</span><h3>{title}</h3><p>Otvoriť zachovanú štúdiu<ArrowUpRight size={17}/></p></Link>)}</div></section><section className="archive-section"><h2>Predchádzajúce zostavy variantu C</h2><div className="archive-grid">{[['a','a'],['a','b'],['b','a']].map(([heating,living])=><Link key={`${heating}${living}`} href={`/archiv/podorys?variant=c&heating=${heating}&living=${living}`}><span>ARCHÍV ZOSTAVY</span><h3>C / {heating.toUpperCase()} / {living.toUpperCase()}</h3><p>Technická {heating.toUpperCase()} · obývačka {living.toUpperCase()}<ArrowUpRight size={17}/></p></Link>)}</div></section><section className="archive-section"><h2>Uložené verzie projektu</h2><p>Nemenné historické snímky. Obsahujú dobový model aj jeho rozhranie.</p><div className="archive-grid">{['v1','v2','archiv/model'].map(v=><Link key={v} href={`/${v}`}><span>HISTORICKÁ SNÍMKA</span><h3>{v==='archiv/model'?'Pôvodný vstup do modelu':v.toUpperCase()}</h3><p>Otvoriť uloženú verziu<ArrowUpRight size={17}/></p></Link>)}</div></section></main></div>;}
